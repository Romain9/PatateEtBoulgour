{{>components/header}}

{{#user}}
    <div class="entetePr">
        <h1 class="name">Profil de {{firstName}}, {{lastName}}</h1>
        <p class="age">{{age}} ans</p>
        <p class="gender">{{gender}}.</p>
        {{#pathology}}<p class="pathology">Pathologie: {{label}}.</p>{{/pathology}}
        <p class="adresse">{{address}}</p>

        {{#isAdmin}}
            <a class="adminBtn" href="/admin/utilisateurs-liste">Consulter la liste des utilisateurs</a>
            <a class="adminBtn" href="/admin/consulter-activités">Consulter la note moyenne des activités</a>
            <a class="adminBtn" href="/admin/consulter-parcours">Consulter la liste des parcours réalisés par les utilisateurs</a>
        {{/isAdmin}}
    </div>
{{/user}}

{{^isAdmin}}
    {{#success}}
        <p class="success"><strong>Youpi:</strong> {{this}}</p>
    {{/success}}

    {{#errors}}
        <p class="error"><strong>Erreur:</strong> {{this}}</p>
    {{/errors}}

    {{>components/recommend}}

    <div>
        {{#allPathologies}}
            <p>{{label}}</p>
        {{/allPathologies}}
    </div>

    <h2 class="act">Vos activités</h2>

    {{>components/activities}}


    {{>components/rateParcours}}
{{/isAdmin}}


{{>components/footer}}